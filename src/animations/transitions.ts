import type { TransitionProps } from 'vue'

export const fadeTransition: TransitionProps = {
  name: 'v-fade',
  css: true,
}

export const slideUpTransition: TransitionProps = {
  name: 'v-slide-up',
  css: true,
}

export const scaleTransition: TransitionProps = {
  name: 'v-scale',
  css: true,
}

export const collapseTransition: TransitionProps = {
  name: 'v-collapse',
  css: true,
}
